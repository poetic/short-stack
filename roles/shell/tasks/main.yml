---
- name: install/update commonly used command line tools
  homebrew: name={{ item }} state=latest
  with_items:
    - autojump
    - chromedriver
    - ctags
    - hub
    - irssi
    - reattach-to-user-namespace
    - the_silver_searcher
    - wget

- name: add shell configuration to zsh
  copy: src=shell.zsh dest={{ zshrc_d }}
